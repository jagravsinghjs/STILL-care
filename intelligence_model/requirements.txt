# --- API layer ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.12.2

# --- DB / migrations ---
# sqlite3 is stdlib, no package needed

# --- Module 11: report generation ---
reportlab==4.2.5

# --- Module 12/13: numeric + heuristic/ML ---
numpy==2.2.1
scikit-learn==1.6.0        # only needed once train/train_model.py exists; safe to install now

# --- C++ core bindings (build-time, not runtime) ---
pybind11==2.13.6

# --- Testing ---
pytest==8.3.4
httpx==0.28.1               # required by fastapi.testclient / async test calls

# --- Dev convenience ---
python-dotenv==1.0.1         # loads DB path / config from a .env file instead of hardcoding