import {useState} from 'react';import {useSearchParams} from 'react-router-dom';import {Send,MessageCircle} from 'lucide-react';import {useApp} from '../../store/useApp';import {users} from '../../data/mock';import {Button,Card} from '../../components/ui';
export function Messages(){const role=useApp(s=>s.role)!;const [params,setParams]=useSearchParams();const selected=params.get('user');const id=role==='user'?'ananya':users.some(s=>s.id===selected)?selected!:'ananya';const allMessages=useApp(s=>s.messages);const messages=allMessages.filter(m=>m.userId===id);const send=useApp(s=>s.send);const [text,setText]=useState('');const person=role==='user'?'Dr. Meera Iyer':users.find(s=>s.id===id)!.name;function submit(e:React.FormEvent){e.preventDefault();if(!text.trim())return;send({id:crypto.randomUUID(),userId:id,sender:role,text:text.trim(),date:new Date().toISOString()});setText('')}
return <><span className="eyebrow">STAY CONNECTED</span><h1>There’s space to talk.</h1><p className="lead">A conversation with {role==='user'?'your supervisor':'the people you support'}, at a human pace.</p>{role==='supervisor'&&<label className="user-select">Conversation<select value={id} onChange={e=>{setParams({user:e.target.value});setText('')}}>{users.map(s=><option key={s.id} value={s.id}>{s.name}</option>)}</select></label>}<Card className="chat"><div className="chat-header"><span className="avatar">{role==='user'?'MI':users.find(s=>s.id===id)!.initials}</span><div><h3>{person}</h3><small>Your conversation</small></div><MessageCircle size={22}/></div><div className="chat-body" aria-live="polite">{messages.length===0&&<p className="empty">Start a supportive conversation with {person.split(' ')[0]}.</p>}{messages.map(m=><div key={m.id} className={`bubble ${m.sender===role?'own':''}`}><p>{m.text}</p><small>{m.sender===role?'You':person} · {new Date(m.date).toLocaleTimeString('en-IN',{hour:'2-digit',minute:'2-digit'})}</small></div>)}</div><form onSubmit={submit} className="chat-compose"><label className="sr-only" htmlFor="message">Your message</label><input id="message" value={text} onChange={e=>setText(e.target.value)} placeholder="Write a message…" maxLength={2000}/><Button disabled={!text.trim()} type="submit" aria-label="Send message"><Send size={19}/></Button></form><p className="chat-note">Messages are not delivered to a real person. Not monitored for urgent support.</p></Card></>}


