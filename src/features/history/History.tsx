import {useState} from 'react';import {PenLine,Mic,ChevronDown} from 'lucide-react';import {useApp} from '../../store/useApp';import {dateLabel} from '../../utils/date';import {Card} from '../../components/ui';
export function History(){const entries=useApp(s=>s.reflections);const [open,setOpen]=useState<string|null>(null);return <><div className="eyebrow">YOUR CONTINUITY</div><h1>Small moments, an ongoing story.</h1><p className="lead">A place to look back, without measuring yourself.</p><Card><div className="section-heading"><h2>Your check-ins</h2><span>Most recent first</span></div>{entries.map(r=><article className="history-item" key={r.id}><span className="tile-icon lavender">{r.mode==='Written'?<PenLine size={19}/>:<Mic size={19}/>}</span><div className="grow"><div className="muted small-text">{dateLabel(r.date)} · {r.mode}</div><h3>{r.observation}</h3><button className="text-link" aria-expanded={open===r.id} onClick={()=>setOpen(open===r.id?null:r.id)}>{open===r.id?'Hide details':'View details'}<ChevronDown size={15}/></button>{open===r.id&&<div className="private-detail"><span className="eyebrow">YOUR PRIVATE REFLECTION</span><p className="reflection-text">{r.raw}</p><small>This text does not appear in counsellor views.</small></div>}</div></article>)}</Card></>}
