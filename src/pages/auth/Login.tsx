import {useState} from 'react';import {useNavigate} from 'react-router-dom';import {Leaf,ArrowRight} from 'lucide-react';import {useApp} from '../../store/useApp';import {Button} from '../../components/ui';import {Botanical} from '../../components/shared/Botanical';
export function Login(){const [role,setRole]=useState<'user'|'supervisor'>('user');const login=useApp(s=>s.login);const navigate=useNavigate();return <div className="login-page"><div className="login-story"><div className="brand"><Leaf/> STILL-care</div><div><span className="eyebrow">A STEADY CONNECTION</span><h1>You don’t have to<br/>hold it all alone.</h1><p>A space to reflect, find your rhythm, and stay connected with someone who cares.</p><Botanical/></div><small>Safety, Trust & Intervention for Longitudinal Life-Care</small></div><div className="login-form"><span className="eyebrow">WELCOME TO STILL-CARE</span><h2>A little space for you.</h2><p>Choose your space to continue.</p><div className="segmented"><button className={role==='user'?'selected':''} onClick={()=>setRole('user')}>User Login</button><button className={role==='supervisor'?'selected':''} onClick={()=>setRole('supervisor')}>Counselor Login</button></div><label>Your profile<input readOnly value={role==='user'?'Ananya Sharma':'Dr. Meera Iyer'}/></label><Button onClick={()=>{login(role);navigate(role==='user'?'/patient/dashboard':'/supervisor/dashboard')}}>Continue as {role==='user'?'user':'counsellor'}<ArrowRight size={18}/></Button></div></div>}
