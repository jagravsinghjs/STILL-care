import {Link,useNavigate} from 'react-router-dom';import {ArrowRight,LogOut,LockKeyhole} from 'lucide-react';import {Card,Button} from '../../components/ui';import {useApp} from '../../store/useApp';
export function Profile({supervisor=false}:{supervisor?:boolean}){const logout=useApp(s=>s.logout);const navigate=useNavigate();return <div className="narrow"><span className="eyebrow">{supervisor?'YOUR SUPPORT CONNECTION':'YOUR PERSONAL SPACE'}</span><h1>{supervisor?'A familiar person to turn to.':'Your profile.'}</h1><p className="lead">{supervisor?'Support can start with a simple hello.':'The essentials, and clarity about your information.'}</p><Card><div className="supervisor-person"><span className="avatar portrait">{supervisor?'MI':'AS'}</span><div><h2>{supervisor?'Dr. Meera Iyer':'Ananya Sharma'}</h2><p className="muted">{supervisor?'Assigned supervisor':'Personal wellbeing'}</p></div></div>{!supervisor&&<dl><dt>User</dt><dd>Ananya Sharma</dd><dt>Supervisor</dt><dd>Dr. Meera Iyer</dd></dl>}<p className="muted">Stay connected through the busy weeks, everyday responsibilities, and changes in routine.</p><Link to="/patient/messages" className="button">Message your supervisor <ArrowRight size={17}/></Link></Card><Card className="privacy-card"><LockKeyhole size={22}/><h2>What’s private, what’s shared.</h2><p>Your reflection text and simulated transcripts appear only in your personal view. Counsellor pages show high-level continuity observations. Messages are visible to both roles.</p><p>New entries reset when the page refreshes.</p></Card>{!supervisor&&<Button className="secondary" onClick={()=>{logout();navigate('/login')}}><LogOut size={17}/> Log out</Button>}</div>}
