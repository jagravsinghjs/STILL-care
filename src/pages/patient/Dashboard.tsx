import {Link} from 'react-router-dom';import {ArrowRight,ArrowUpRight,PenLine,LockKeyhole,MessageCircle,Mic,Leaf} from 'lucide-react';import {Card} from '../../components/ui';import {Botanical} from '../../components/shared/Botanical';import {useApp} from '../../store/useApp';import {dateLabel} from '../../utils/date';
export function Dashboard(){const recent=useApp(s=>s.reflections[0]);const messages=useApp(s=>s.messages);const last=messages.filter(m=>m.userId==='ananya').at(-1);return <><div className="page-heading"><div><div className="eyebrow">{new Date().toLocaleDateString('en-IN',{weekday:'long',day:'numeric',month:'long'}).toUpperCase()}</div><h1>Good morning, Ananya<span className="heading-dot">.</span></h1><p>There’s room for whatever today feels like.</p></div><span className="gentle-label"><span/> A moment for yourself</span></div><section className="checkin-hero"><div className="hero-copy"><div className="eyebrow"><Leaf size={15}/> CHECK IN WITH YOURSELF</div><h2>How are things<br/>going today?</h2><p>A busy mind, a small win, or just an ordinary day.<br className="desktop-break"/> You can start wherever you are.</p><Link className="button" to="/patient/checkin/new">Start a check-in <ArrowRight size={18}/></Link><div className="hero-meta"><span><PenLine size={14}/> Write</span><span className="divider"/><span><Mic size={14}/> Voice</span><span>·</span><span>At your own pace</span></div></div><Botanical/></section><div className="privacy-line"><LockKeyhole size={15}/><span>Your words stay in your personal view. Your supervisor sees only continuity summaries.</span></div><div className="section-heading"><h2>Your space, at a glance</h2><span>A little continuity goes a long way</span></div><div className="dashboard-grid"><Card><div className="card-heading"><span className="tile-icon lavender"><PenLine size={19}/></span><span className="eyebrow">RECENT CHECK-IN</span><span className="subtle-right">{recent.mode}</span></div><p className="date">{dateLabel(recent.date)}</p><h3>{recent.observation}</h3><p className="muted">One moment in your ongoing story.</p><Link className="text-link" to="/patient/history">View your history <ArrowRight size={16}/></Link></Card><Card><div className="card-heading"><span className="tile-icon peach"><UsersIcon/></span><span className="eyebrow">YOUR SUPERVISOR</span></div><div className="supervisor-person"><span className="avatar portrait">MI</span><div><h3>Dr. Meera Iyer</h3><p className="muted">Your continuity of support</p></div></div><p className="muted">A familiar person to turn to, through the busy days and the quieter ones.</p><Link className="text-link" to="/patient/supervisor">Connect with your supervisor <ArrowUpRight size={16}/></Link></Card></div><Card className="message-preview"><span className="tile-icon blue"><MessageCircle size={20}/></span><div><div className="card-heading"><h3>A conversation, whenever you need it</h3></div><p className="muted">{last?.text??'Your conversation with Dr. Meera Iyer starts here.'}</p></div><Link className="text-link" to="/patient/messages">Open messages <ArrowRight size={17}/></Link></Card><div className="closing-note"><Leaf size={16}/> You don’t need the perfect words. Just a place to begin.</div></>}
function UsersIcon(){return <span aria-hidden="true">♡</span>}

